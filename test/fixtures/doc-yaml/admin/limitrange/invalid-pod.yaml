apiVersion: v1
kind: Pod
metadata:
  name: invalid-pod
spec:
  containers:
  - name: kubernetes-serve-hostname
    image: harbor.ultra.com/k8s/serve_hostname
    resources:
      limits:
        cpu: "3"
        memory: 100Mi
